
# 主打一个能跑就行
```java
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        if (n!=0){
          System.arraycopy(nums2,0,nums1,m,nums2.length);
        }

      if (m==0){
    nums1=nums2;
}
//sort
      Arrays.sort(nums1);
  }
```